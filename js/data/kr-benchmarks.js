// ============================================================
// kr-benchmarks.js — 국내 매체 참고 단가 (순수 데이터)
//
// 왜 필요한가
//   MM_DATA(구글·Meta 업종 벤치마크)에는 네이버·카카오·크리테오가 없다. 그래서 국내 믹스를
//   짜려면 매번 단가를 손으로 넣어야 했다. 이 파일이 그 빈칸의 '출발점'을 채운다.
//
// 무엇을 담았나 / 담지 않았나
//   ✅ 단가(CPC/CPM/건당)와 CTR — 시장 단가 수준. 허브가 이미 공개하는 benchmarkData 와 같은 성격.
//   ❌ CVR·객단가·매출 — 넣지 않는다. 전환은 광고주 사업 성과라 브랜드마다 다르고,
//      여기 값을 그대로 쓰면 근거 없는 매출 추정이 만들어진다. 각자 실적을 플래너에 넣어야 한다.
//   ❌ 브랜드명·캠페인명 — 어떤 광고주 자료인지 드러내지 않는다. 이 저장소는 공개다.
//
// 어떻게 뽑았나
//   2024~2026 집행 미디어믹스 산출물의 매체별 예상 단가를 상품 단위로 모아 중앙값을 냈다.
//   개별 브랜드 수치는 남기지 않고, 표본 수와 관측 범위만 note 에 적는다.
//   표본이 얕은 상품은 sourceKind 를 '가정'으로 낮춰 엔진이 뒤로 밀도록 했다
//   (엔진 근거 등급: 실적 > 과거 제안 > 참고값 > 가정).
//
// 주의
//   · 업종은 전부 '전 업종(통합)'이다. 업종별 값은 각자 실적으로 채워야 한다.
//     (업종을 고르면 mix-autoplan 의 전 업종 대체 규칙으로 자동 연결된다)
//   · 원자료 일부는 대행 마크업이 포함된 공급가 기준이라, 단가는 실매체비의 상한으로 보는 게 안전하다.
//   · 매체 정책·경쟁 상황에 따라 수시로 변한다. 분기마다 자사 실적으로 갱신할 것.
//
// 형식은 mix-engine 의 validatePack 벤치마크 레코드와 같다.
// ============================================================
(function () {
  'use strict';

  var COMMON = { brand: '국내 매체 참고', industry: '전 업종(통합)', device: '전체', costBasis: 'media-net', publicReference: true };

  function rec(id, product, model, rate, ctr, kind, note) {
    var o = {};
    for (var k in COMMON) o[k] = COMMON[k];
    o.id = 'kr-' + id;
    o.product = product;
    o.model = model;
    o.rate = rate == null ? '' : rate;
    o.ctr = ctr == null ? '' : ctr;
    o.sourceKind = kind;                 // '참고값' = 표본 충분 / '가정' = 표본 얕음
    o.sourceDate = '';                   // 기준일은 사용자가 확인해 채운다
    o.source = '국내 매체 참고 단가 (2024~2026 집행 미디어믹스 집계, 특정 광고주 실적 아님)';
    o.note = note;
    return o;
  }

  window.MM_KR_BENCH = [
    // ── 표본이 충분한 것(복수 브랜드·다회 집행) ────────────────
    rec('kakao-biz', 'kakao-biz', 'CPC', 150, 0.80, '참고값',
      '표본 83행 · 3개 브랜드. 관측 CPC 120~420원으로 폭이 크다(소재·타깃·시즌 영향). 자사 실적이 있으면 반드시 그 값을 쓸 것.'),
    rec('meta-traffic', 'meta-traffic', 'CPC', 260, 2.70, '참고값',
      '표본 86행 · 2개 브랜드. 관측 CPC 200~390원. 소재 품질에 따른 편차가 가장 큰 매체다.'),
    rec('naver-gfa', 'naver-gfa', 'CPC', 175, 2.00, '참고값',
      '표본 23행 · 단일 브랜드. 브랜드 편중이 있어 다른 업종에는 그대로 적용하기 어렵다.'),
    rec('kakao-display', 'kakao-display', 'CPC', 180, 0.40, '참고값',
      '표본 18행 · 2개 브랜드. 관측 CPC 115~205원.'),

    // ── 표본이 얕은 것(1~2개 브랜드, 소수 행) — 엔진에서 뒤로 밀린다 ──
    rec('naver-search', 'naver-search', 'CPC', 450, 1.60, '가정',
      '표본 2개 브랜드. 관측 CPC 365~610원. 검색은 키워드 구성에 따라 배 이상 갈리므로 실제 키워드 단가로 대체할 것.'),
    rec('naver-shopping', 'naver-shopping', 'CPC', 180, 1.50, '가정',
      '표본 1개 브랜드. 카테고리 경쟁도에 따라 크게 달라진다.'),
    rec('kakao-search', 'kakao-search', 'CPC', 440, null, '가정',
      '다음 검색광고. 표본 2개 브랜드, 관측 CPC 370~515원. CTR 은 표본이 한쪽에 쏠려 넣지 않았다.'),
    rec('criteo-lf', 'criteo-lf', 'CPC', 100, null, '가정',
      '표본 1개 브랜드. 리타겟팅(LF)·신규(CCA)·유사(LAL) 관측 CPC 90~130원. 모수 크기에 따라 달라진다.'),
    rec('criteo-cca', 'criteo-cca', 'CPC', 90, null, '가정',
      '표본 1개 브랜드. 장기 미접속·신규 타깃.'),
    rec('criteo-lal', 'criteo-lal', 'CPC', 95, null, '가정',
      '표본 1개 브랜드. 유사 타깃.'),
    rec('google-demand', 'google-demand', 'CPC', 135, null, '가정',
      '표본 1개 브랜드. 디맨드젠은 지면 통합이라 편차가 크다.'),
    rec('kakao-message', 'kakao-message', 'SEND', 20, null, '가정',
      '건당 발송 단가. 표본 1개 브랜드. 발송량은 친구 수 상한을 넘을 수 없으니 예산만으로 역산하지 말 것.'),

    // ── 정액 상품 — 단가를 넣지 않는다 ──────────────────────
    rec('naver-brand', 'naver-brand', 'FIXED', null, null, '가정',
      '브랜드검색은 쿼리수 구간별 기간 정액 계약이라 CPC 개념이 없다. 계약 단가와 예상 노출·클릭을 견적서에서 직접 넣어야 한다.')
  ];
})();
